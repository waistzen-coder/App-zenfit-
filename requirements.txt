# Las dependencias del fichaje. Cada una con su motivo, porque una dependencia
# que nadie sabe por qué está acaba quedándose para siempre.

psycopg[binary]==3.3.5      # PostgreSQL. Sin ORM: el SQL se escribe a mano.
yoyo-migrations==9.0.0      # migraciones en SQL a secas, sin arrastrar SQLAlchemy
flask==3.1.3                # las dos webs, renderizadas en el servidor
segno==1.6.6                # los QR, en SVG y sin dependencias propias

# El PIN y las contraseñas usan scrypt de la biblioteca estándar. Lo más
# delicado del sistema no depende de nadie.
